{
  "name": "docker-deploy",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:migrate": "turbo run migrate --filter=@repo/db",
    "db:generate": "turbo run generate --filter=@repo/db",
    "start:backend": "bun run --cwd apps/backend dev",
    "start:ws": "bun run --cwd apps/ws-backend dev",
    "start:web": "bun run --cwd apps/web dev"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.9.3",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
